export { light, dark, category } from './colors';
export type { CategoryName, Colors } from './colors';
export { typography, fontFamily } from './typography';
export { spacing, radius, screenPadding, cardPadding, rowHeight, tabBarHeight, fabSize } from './spacing';

import { light } from './colors';
export const colors = light;
